name: CI
on:
  push:
    branches:
      - '**'
    paths-ignore:
      - '.gitignore'
      - '**.md'
      - 'LICENSE'
      - '.github/dependabot.yml'
      - '.github/workflows/manual.yml'

  pull_request:
    branches:
      - '**'
    paths-ignore:
      - '.gitignore'
      - '**.md'
      - 'LICENSE'
      - '.github/dependabot.yml'
      - '.github/workflows/manual.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'master' }}

jobs:

  perf-run:
    # This will cause the job being skipped on PRs originating from the main repository
    # which is OK, because in that case the regular `push` trigger will run the workflow
    if: ${{ github.head_ref == '' && github.repository == 'jajik/mod_proxy_cluster-perf-comparison' }}
    name: ${{ contains(matrix.os, 'arm') && 'ARM' || 'x86' }} (${{ matrix.tomcat_count }} tomcats, ${{ matrix.tomcat_affected }} affected by ${{ matrix.tomcat_action }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - tomcat_count: 2
            os: ubuntu-latest
          - tomcat_count: 20
            os: ubuntu-latest
          - tomcat_count: 50
            os: ubuntu-latest

          - tomcat_count: 2
            shutdown: 1
            os: ubuntu-latest
          - tomcat_count: 2
            disable: 1
            os: ubuntu-latest

          - tomcat_count: 50
            shutdown: 25
            os: ubuntu-latest
          - tomcat_count: 50
            disable: 25
            os: ubuntu-latest
          - tomcat_count: 20
            shutdown: 10
            os: ubuntu-latest
          - tomcat_count: 20
            disable: 10
            os: ubuntu-latest

          - tomcat_count: 50
            shutdown: 35
            os: ubuntu-latest
          - tomcat_count: 50
            disable: 35
            os: ubuntu-latest
          - tomcat_count: 50
            shutdown: 35
            os: ubuntu-24.04-arm
          - tomcat_count: 50
            disable: 35
            os: ubuntu-24.04-arm

          - tomcat_count: 20
            shutdown: 14
            os: ubuntu-latest
          - tomcat_count: 20
            disable: 14
            os: ubuntu-latest
          - tomcat_count: 20
            shutdown: 14
            os: ubuntu-24.04-arm
          - tomcat_count: 20
            disable: 14
            os: ubuntu-24.04-arm

    env:
      TOMCAT_COUNT:      ${{ matrix.tomcat_count }}
      SHUTDOWN_RANDOMLY: ${{ matrix.shutdown || 0 }}
      DISABLE_RANDOMLY:  ${{ matrix.disable  || 0 }}
      STOP_RANDOMLY:     ${{ matrix.stop || 0 }}
      KILL_RANDOMLY:     ${{ matrix.kill || 0 }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Checkout submodules
        run: git submodule update --init --recursive
      - name: Set the httpd logging level
        run: |
          if [ ${{ matrix.tomcat_count }} -le 30 ]; then echo "HTTPD_LOG_LEVEL=debug" >> $GITHUB_ENV; fi
      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17
      - name: Get dependencies and remove unused packages
        run: |
          sudo apt-get update -y
          sudo apt-get install gcc maven git curl iproute2 wcstools g++ cmake liblist-moreutils-perl
          sudo rm -rf /usr/local/.ghcup /usr/local/lib/android /usr/share/swift /opt/microsoft/msedge/ /opt/google/chrome/
      - name: Setup  performance tests
        run: |
          sh setup.sh
          # create a directory on a filesystem with more space
          sudo mkdir /mnt/output
          sudo chmod 777 /mnt/output
          ln -s /mnt/output output
      - name: Run performance tests
        run: sh run-suite.sh 2>&1 | tee output/run-suite.logs
      - name: Make a summary of test runs
        run: |
          perl summary.pl output/ | tee summary
          cp summary output/summary-for-${{ contains(matrix.os, 'arm') && 'ARM' || 'x86' }}-${{ matrix.tomcat_count }}-tomcats-${{ matrix.tomcat_affected }}-affected-by-${{ matrix.tomcat_action }}
          # preserve environment variables as well
          printenv > output/envvars
      - name: Check Status 200
        run: |
          if [ $(grep -c "Status 200" summary) -eq 0 ]; then exit 1; fi
      - name: Preserve output
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: Output-for-${{ contains(matrix.os, 'arm') && 'ARM' || 'x86' }}-${{ matrix.tomcat_count }}-tomcats-${{ matrix.tomcat_affected }}-affected-by-${{ matrix.tomcat_action }}
          path: output/*

